# Validation Evidence Policy

Evidence is criterion-specific, redacted, and derived from read-only product sources. Never persist production payloads, credentials, JWTs, private keys, real CNPJ, provider identifiers, or captured document URLs. Privacy mutation samples (including valid CNPJ) are constructed only at runtime in temporary test directories.
